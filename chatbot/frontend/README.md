# Porto Chatbot Frontend

Next.js + React + assistant-ui frontend for Porto Chatbot.

```bash
npm install
npm run dev
```

The dev server proxies `/api/*` to `http://127.0.0.1:8100` by default. Override with:

```bash
PORTO_API_BASE_URL=http://127.0.0.1:8100 npm run dev
```
